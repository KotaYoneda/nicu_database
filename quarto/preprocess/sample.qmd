---
title: " Quarto Document"
author: "Kota Yoneda"
format: html
---

## 変数設定

```{r}
rm(list=ls()); gc(); gc();
root = "C:/Users/kotay/R/r_template/"
# root = "/Users/kota/Documents/R/r_template""

key = ""
```

## パッケージの読み込み

```{r}
pacman::p_load(
    tidyverse,
    magrittr
)
```

## 初期設定

```{r}
root |> stringr::str_c("common/utils.R") |> source()

src_dir <- 
    root |> 
    stringr::str_c("data/public/src/")

table_dir <- 
    root |> 
    stringr::str_c("output/tables/", key, "/")

table_dir |> 
    purrr::map_chr(make_dir)
```

## データの読み込み

```{r}

```

## データの変換

```{r}

```

## ?? の作成

```{r}

```

## ?? の保存

```{r}

```